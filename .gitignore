# secrets
.env
.env.local

# python
.venv/
__pycache__/
*.pyc

# models (downloaded by scripts/fetch_model.py)
backend/models/*.task

# runtime artifacts
backend/storage/uploads/*
backend/storage/artifacts/*
!backend/storage/**/.gitkeep

# node / next
node_modules/
.next/
next-env.d.ts

# evidence: keep the reports and notes, not the source video files
EVIDENCE/**/*.mp4
EVIDENCE/**/*.webm
EVIDENCE/**/smoke/

# test footage fetched by scripts/fetch_test_clip.py. Third-party video, and this repository is
# public: the script records how to obtain a clip, which is the reproducible part. The URL and the
# measurements it produced belong in BUILD_NOTES.md, the file itself does not belong in git.
samples/
